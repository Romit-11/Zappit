<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Hotels – Zappit</title>
  <link rel="stylesheet" href="/style.css">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
</head>
<body class="hotels-bg">

  <%- include("partials/navbar") %>

  <!-- Page Header -->
  <div class="page-header">
    <span class="section-label" style="color:var(--orange-light)">Accommodations</span>
    <h2>Hotels &amp; Stays</h2>
    <p>Handpicked accommodations with premium amenities for the perfect trip</p>
  </div>

  <div class="container">

    <% if (localHotels && localHotels.length > 0) { %>
      <% localHotels.forEach(hotel => { %>
        <div class="hotel-card">
          <h3><%= hotel.name %></h3>
          <p><i class="fa-solid fa-location-dot" style="color:var(--red)"></i> <%= hotel.location %></p>
          <p style="font-size:15px;font-weight:600;color:var(--orange-dark)">💰 ₹ <%= hotel.price %> <span style="font-size:12px;color:var(--text-muted);font-weight:400">/ night</span></p>
          <a href="https://www.booking.com/searchresults.html?ss=<%= encodeURIComponent(hotel.location) %>" target="_blank">
            <button class="book-btn">🏨 Book Hotel →</button>
          </a>
        </div>
      <% }) %>
    <% } else { %>
      <p class="empty-msg" style="color:rgba(255,255,255,.7);text-align:center;padding:60px 0">No hotels available yet.</p>
    <% } %>

  </div>

  <footer class="footer">
    <strong>Zappit</strong>
    <p>© 2025 Zappit. All Rights Reserved.</p>
  </footer>

</body>
</html>
